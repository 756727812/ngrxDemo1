import { TextAvatarPipe } from './text-avatar.pipe';

describe('TextAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new TextAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
